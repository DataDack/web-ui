{
  "name": "@serverless-ui/typescript-config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript presets for every workspace in serverless-ui.",
  "license": "MIT",
  "files": [
    "base.json",
    "react-library.json",
    "vite-app.json"
  ],
  "nx": {
    "tags": [
      "scope:config"
    ]
  }
}
